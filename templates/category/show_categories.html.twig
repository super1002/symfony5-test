{% extends 'base.html.twig' %}

{% block title %}Категории{% endblock %}

{% block content %}
    <h1>Категории</h1>
    <ul>
        {% for category in categories %}
            <li><a href="{{ path('show_cat_videos', {'categoryId': category.id}) }}">{{ category.title }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
